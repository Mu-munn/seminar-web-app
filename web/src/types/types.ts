export interface Profile {
    id: string
    full_name: string
    course: string
    grade: string
    class: string
    class_number: number
    student_number: string
    isAdmin: boolean
}